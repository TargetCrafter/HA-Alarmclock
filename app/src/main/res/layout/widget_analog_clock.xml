<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- The next-alarm indicator is baked directly into the bitmap (see AnalogClockRenderer) so
         it can sit centered on the face with an opaque patch masking the hands behind it — not
         achievable with a separate RemoteViews TextView layered on top. -->
    <ImageView
        android:id="@+id/widget_analog_face"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@null"
        android:scaleType="fitCenter" />

</FrameLayout>
